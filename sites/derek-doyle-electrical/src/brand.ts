// Written by SiteForge: set to "/brand-logo.png" when the lead's own logo
// should show in the Nav and Footer ("Use their logo" on the pipeline card).
export const BRAND_LOGO: string | null = null;
